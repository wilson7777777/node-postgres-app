apiVersion: v1
kind: Service
metadata:
  name: my-node-postgres-app-service
spec:
  selector:
    app: my-node-postgres-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
